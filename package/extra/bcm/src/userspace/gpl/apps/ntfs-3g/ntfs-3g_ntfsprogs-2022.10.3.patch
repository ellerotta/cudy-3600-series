--- ori_ntfs-3g_ntfsprogs-2022.10.3/src/ntfs-3g.c	2022-10-20 23:33:44.000000000 +0800
+++ ntfs-3g_ntfsprogs-2022.10.3/src/ntfs-3g.c	2022-11-08 16:48:57.467146107 +0800
@@ -4263,7 +4263,9 @@
 		    || (len >= (int)sizeof(buf))
 		    || (fuse_opt_add_arg(&args, buf) == -1))
 			goto err;
-	} else {
+	}
+#if 0 // brcm: no default option, all from command line. e.g. hotplug	 
+	else {	
 #if !CACHEING
 		if (fuse_opt_add_arg(&args, "-ouse_ino,kernel_cache"
 				",attr_timeout=0") == -1)
@@ -4274,6 +4276,7 @@
 			goto err;
 #endif
 	}
+#endif		
 	if (ctx->debug)
 		if (fuse_opt_add_arg(&args, "-odebug") == -1)
 			goto err;
